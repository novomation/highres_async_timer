[package]
name = "highres_async_timer"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = { version = "0.2" }
tokio = { version = "1.53", features = ["net", "rt"] }

[dev-dependencies]
tokio = { version = "1.53", features = ["macros", "net", "rt"] }
